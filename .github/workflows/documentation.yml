name: Generate documentation

on:
  schedule:
    - cron: "0 0 * * 1"
  workflow_dispatch: {}

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        name: Checkout Repository
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17.0.10+7'
          distribution: 'temurin'
      - name: Generate Javadoc
        run: ./gradlew javadoc
      - name: Deploy Javadoc
        uses: JamesIves/github-pages-deploy-action@619f45e23791f22792d0961d34164e1c2c48ee7d
        with:
          branch: javadoc
          clean: true
          folder: build/docs/javadoc
          target-folder: .
          git-config-name: "github-actions"
          git-config-email: "<>"
